namespace = add_son_of_immortality

add_son_of_immortality.01={
	hidden = yes
	immediate={

		if = {
			limit = {
				mother ?= {
					has_trait = immortality21
				}
				has_game_rule = is_yes_all
			}
			add_trait = son_of_immortality1
		}
		else_if = {
			limit = {
				real_father ?= {
					has_trait = immortality21
				}
				has_game_rule = is_yes_all
			}
			add_trait = son_of_immortality1
		}
		else_if = {
			limit = {
				real_father ?= {
					has_trait = immortality21
				}
				has_game_rule = is_yes_fa
			}
			add_trait = son_of_immortality2
		}
	}
}